import { NextPage } from "next";

export const Settings: NextPage = () => {
  return (
    <div className="w-full flex justify-center pt-8 text-lg font-bold text-slate-700">
      <p>設定画面になる予定です。</p>
    </div>
  );
};

export default Settings;
